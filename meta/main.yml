---
galaxy_info:
  author: David Reagan
  description: Install and configure Apache 2.4
  company: Lane Community College
  license: MIT
  platforms:
  - name: CentOS
    version:
    - 7
  - name: Ubuntu
    versions:
    - trusty
    - xenial
    - bionic
  - name: Debian
    versions:
    - stretch
  - name: OracleLinux
    versions:
      - 7
  categories:
  - development
  - web
dependencies:
  - { role: aspects_iptables, when: aspects_iptables_enabled }
  - { role: aspects_packages, when: aspects_packages_enabled }
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
